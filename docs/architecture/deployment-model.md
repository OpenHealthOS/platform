# Deployment Model

## Local Development
Use Docker Compose for local orchestration and rapid iteration.

## Cloud Target
Azure Container Apps are the initial target deployment environment for the platform.

## Future Direction
The architecture should remain cloud-agnostic so it can later support AKS, Kubernetes, AWS, or GCP if needed.

## Design Goal
The platform should be deployable across environments without requiring a full redesign of the application architecture.
